--- scalalib/target/scalaSources/2.11.12/scala/collection/immutable/Range.scala	2017-11-03 00:26:48.000000000 +0100
+++ scalalib/overrides-2.11/scala/collection/immutable/Range.scala	2021-02-20 13:54:41.643200221 +0100
@@ -141,3 +141,13 @@
 
-  private def description = "%d %s %d by %s".format(start, if (isInclusive) "to" else "until", end, step)
+  private def description = {
+    val sb = new StringBuilder
+    sb.append(start)
+    sb.append(" ")
+    sb.append(if (isInclusive) "to" else "until")
+    sb.append(" ")
+    sb.append(end)
+    sb.append(" by ")
+    sb.append(step)
+    sb.toString
+  }
   private def fail() = throw new IllegalArgumentException(description + ": seqs cannot contain more than Int.MaxValue elements.")
