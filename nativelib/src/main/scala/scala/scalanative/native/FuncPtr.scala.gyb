package scala.scalanative
package native

% for N in range(0, 23):
%   args = ", ".join("arg" + str(i) + ": T" + str(i) for i in range(1, N+1))

trait FuncPtr${N}[${", ".join(["T" + str(i) for i in range(1, N+1)] + ["R"])}] {
  def apply(${args}): R
}

% end
