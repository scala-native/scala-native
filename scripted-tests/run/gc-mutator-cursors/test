# =============================================================================
# GC Mutator Cursors Tests
# =============================================================================
# The low initial heap forces GC cursor re-initialization to grow the heap after
# a large number of mutator threads have registered.

# -----------------------------------------------------------------------------
# Phase 1: Run with Immix GC
# -----------------------------------------------------------------------------
> set nativeConfig ~= { _.withGC(scala.scalanative.build.GC.immix) }
> runThreadBurst

# -----------------------------------------------------------------------------
# Phase 2: Run with Commix GC
# -----------------------------------------------------------------------------
> set nativeConfig ~= { _.withGC(scala.scalanative.build.GC.commix) }
> runThreadBurst
